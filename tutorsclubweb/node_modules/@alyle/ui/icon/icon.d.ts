import { Renderer2, ElementRef, OnChanges, SimpleChanges, OnInit } from '@angular/core';
import { LyIconService, LyIconStyle } from './icon.service';
export declare class Icon implements OnChanges, OnInit {
    private iconService;
    private elementRef;
    private renderer;
    private iconStyle;
    private _defaultClass;
    private _src;
    private _icon;
    src: string;
    icon: string;
    constructor(iconService: LyIconService, elementRef: ElementRef, renderer: Renderer2, iconStyle: LyIconStyle);
    private _isDefault();
    private _prepareSvgIcon(svgIcon);
    private _appendChild(svg);
    private _appendDefaultSvgIcon();
    private _updateClass();
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    /**
     * run only browser
     * remove current icon
     */
    private _cleanIcon();
}
