import { Renderer2, ElementRef } from '@angular/core';
import { LyTheme } from '../theme.service';
export declare class LyShadowService {
    private theme;
    /** Default elevation */
    elevation: number;
    constructor(theme: LyTheme);
    /** demo: setShadow(...[elevation, color]...) */
    setShadow(elementRef: ElementRef, renderer: Renderer2, val: [number, string], oldClassName?: string): string;
}
