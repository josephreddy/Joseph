import { LyTheme } from '../theme.service';
export declare class LyThemeStyles {
    private theme;
    classes: {};
    constructor(theme: LyTheme);
}
export declare class LyGlobalStyles {
    private theme;
    classes: {
        Absolute: string;
    };
    constructor(theme: LyTheme);
}
