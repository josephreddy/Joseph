import { NgZone } from '@angular/core';
import { Observable } from 'rxjs';
export declare class Responsive {
    private _ngZone;
    private platformId;
    private static _queryMap;
    constructor(_ngZone: NgZone, platformId: Object);
    matchMedia(val: string): boolean;
    /**
     * return Observable<boolean>
     */
    observe(value: string): Observable<boolean>;
    private _registerMedia(value);
    stateView(): Observable<any>;
}
