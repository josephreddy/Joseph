import { TemplateRef, ViewContainerRef, OnInit, OnDestroy } from '@angular/core';
import { Subscription } from 'rxjs';
import { Responsive } from '../media.service';
export declare class MediaDirective implements OnInit, OnDestroy {
    private _viewContainer;
    private mediaService;
    private platformId;
    private _media;
    view: Subscription;
    private _TemplateRef;
    lyResponsive: string;
    readonly media: string;
    constructor(_viewContainer: ViewContainerRef, templateRef: TemplateRef<any>, mediaService: Responsive, platformId: Object);
    ngOnInit(): void;
    updateView(): void;
    ngOnDestroy(): void;
}
