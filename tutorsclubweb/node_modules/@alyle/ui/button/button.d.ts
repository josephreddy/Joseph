import { AfterViewInit, ElementRef, Renderer2, NgZone, OnDestroy } from '@angular/core';
import { LyTheme } from '@alyle/ui';
import { LyRippleService } from '@alyle/ui/ripple';
import { LyButtonService } from './button.service';
import { LyBgColorAndRaised } from '@alyle/ui';
export declare class LyButton implements AfterViewInit, OnDestroy {
    private elementRef;
    private renderer;
    private theme;
    rippleStyles: LyRippleService;
    private buttonService;
    private bgAndColor;
    _disabled: boolean;
    private _rippleSensitive;
    private _disabledClassName;
    private _outlinedClassName;
    private _rippleContainer;
    outlined: boolean;
    rippleSensitive: boolean;
    buttonContent: ElementRef;
    disabled: boolean;
    constructor(elementRef: ElementRef, renderer: Renderer2, theme: LyTheme, rippleStyles: LyRippleService, buttonService: LyButtonService, _ngZone: NgZone, bgAndColor: LyBgColorAndRaised);
    focused(): void;
    ngAfterViewInit(): void;
    private disableStyle();
    ngOnDestroy(): void;
}
